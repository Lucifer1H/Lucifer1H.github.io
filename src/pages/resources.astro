---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const resources = await getCollection('resources');
---

<Layout title="资源 | Lucifer H">
  <div class="max-w-4xl mx-auto">
    <div class="mb-12 text-center">
      <h1 class="text-3xl font-bold text-gray-900 dark:text-gray-100 mb-4">资源</h1>
      <p class="text-gray-500 dark:text-gray-400">
        仅作分享与交流，不保证资源的可用性，如有侵权，请联系我删除。
      </p>
    </div>

    <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
      {resources.map((resource) => (
        <a href={resource.data.link} target="_blank" rel="noopener noreferrer" 
           class="group relative flex flex-col h-full bg-white dark:bg-gray-800 rounded-2xl border border-gray-100 dark:border-gray-700 hover:shadow-xl hover:-translate-y-1 transition-all duration-300 overflow-hidden">
          
          {/* Cover Image or Generated Cover */}
          <div class="aspect-[3/2] w-full overflow-hidden bg-gray-100 dark:bg-gray-900 relative">
            {resource.data.cover ? (
              <img src={resource.data.cover} alt={resource.data.title} class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110" />
            ) : (
              <div class="w-full h-full flex items-center justify-center p-6 bg-gradient-to-br from-blue-100 to-indigo-100 dark:from-blue-900/40 dark:to-indigo-900/40">
                {resource.data.icon ? (
                  <span class="text-6xl">{resource.data.icon}</span>
                ) : (
                  <div class="text-center">
                    <div class="w-12 h-16 border-2 border-gray-300 dark:border-gray-600 rounded mx-auto mb-2 flex items-center justify-center">
                      <span class="text-2xl">📖</span>
                    </div>
                  </div>
                )}
              </div>
            )}
            
            {/* Overlay */}
            <div class="absolute inset-0 bg-black/0 group-hover:bg-black/5 transition-colors duration-300"></div>
          </div>

          <div class="p-6 flex flex-col flex-grow">
            <h2 class="text-xl font-bold text-gray-900 dark:text-gray-100 mb-2 group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors line-clamp-2">
              {resource.data.title}
            </h2>
            
            <p class="text-sm text-gray-500 dark:text-gray-400 mb-4 flex-grow line-clamp-3">
              {resource.data.description}
            </p>

            <div class="flex flex-wrap gap-2 pt-2 border-t border-gray-100 dark:border-gray-700/50">
              {resource.data.tags.map((tag) => (
                <span class="px-2 py-0.5 rounded text-xs font-semibold bg-gray-50 dark:bg-gray-700/50 text-gray-600 dark:text-gray-300">
                  #{tag}
                </span>
              ))}
            </div>
          </div>
        </a>
      ))}
    </div>
  </div>
</Layout>
